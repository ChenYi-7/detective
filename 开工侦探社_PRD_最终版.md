# 开工侦探社 PRD v3.0（最终版）

> 本版在 v2.0 基础上完成**交互流程、信息架构、视觉系统的结构性改版（v2.1）**，并融合侦探 noir 视觉示意图。所有页面、动效与视觉元素统一服务于一个核心体验：**帮助用户迈出第一步，并让行动过程逐渐被照亮。**

---

## 执行摘要（TL;DR）

**一句话**：你不是不想做，只是还没找到第一条线索。开工侦探社是一款"先行动、再想目标"的破案式启动器——你可以**直接拍照**，AI 侦探在一面黑暗的推理墙上为你圈出突破口、只派一条 2-5 分钟能做完的线索；做完交证据，吊灯亮一档、黑雾退一片、印章落下；获得第一次行动反馈后，再**填写正式案件目标**（委托案件），AI 据此生成任务树、把推理墙一寸寸照亮。你也可以跳过拍照，从首页直接委托一个目标明确的案件。

**两种入口，一面墙**：
- **快速开工模式（quick_capture）**：先拍照 → 启动线索 → 行动反馈 → 弹出 E-1 委托案件填正式目标；
- **主动委托模式（commissioned）**：首页"新建案件" → E-1 填目标与材料 → AI 生成任务树 → 从任务一开始。
- 两种模式最终都汇入同一面 **推理墙（案件推理墙）**。

**解决什么**：拖延的真正瓶颈是**启动**（task initiation），不是坚持。文字清单对启动障碍者基本失效——goblin.tools 只拆文字、Shelpful 只事后单张验证、Forest/Habitica 奖励与现实脱钩。我们用 **真实物理场景 + 破案叙事 + 前后对比验证 + 动态单步 + 推理墙渐进照亮** 闭环，占住全市场无人同位的格子。

**周末 MVP 边界（技术诚实）**：
- **真跑**：Qwen2.5-VL 单次调用（场景理解 + bbox 圈框 + 任务文案）、像素 diff 前后对比、委托案件的任务树生成（LLM 文本拆解）。
- **脚本化 mock**：无限缩小救援（预设 5 层树）、推理墙复杂关联动画（MVP 只做 2-3 张相纸的单绳悬挂 + 黑雾透明度变化）。
- **热切换**：默认走预设 case 保演示流畅，隐藏键切"真 VLM"演真圈框。

**四条红线**：
1. **拆解质量是及格线**：锚定照片/材料中可见、可执行、可递归，否则被 goblin.tools 淘汰；
2. **零羞耻、零脆断**：断签不归零、叫"案件搁置（cold case）"，删掉一切"连续记录已清零"式文案；
3. **隐私五道闸**：不落库 + 内存 TTL 10 分钟 + 默认本地处理 + 手动涂抹敏感区 + 预录示例桌面；
4. **行动优先于目标**：永远允许用户先拍照、先做一个小动作，再回头补正式目标——不要在入口处逼用户想清楚。

---

## 0 文档信息

| 项目 | 内容 |
|---|---|
| 产品名 | 开工侦探社（Detective for Start） |
| 版本 | v3.0（最终版，融合 v2.1 交互与视觉改版 + 示意图） |
| 日期 | 2026-07-31 |
| 状态 | 可交付 / 进入周末 MVP 开发 |
| 受众 | 产品 / 设计 / 前端 / 后端 / AI 算法 / 美术 / 运营 |
| 一句话 | 你不是不想做，只是还没找到第一条线索。 |
| 相对 v2.0 主要变更 | ① 新增双案件入口（快速开工 / 主动委托）与 E-1 委托案件页；② IA 重构为 A–I 九页面流；③ 案件页升级为"推理墙"渐进照亮；④ 新增登录页（低摩擦）；⑤ 档案库升级为档案文件夹；⑥ 新增视觉风格与美术系统章节；⑦ 状态机扩展 creation_mode / clue_status.blank / is_activation_clue；⑧ MVP 边界重划；⑨ 删除"不登录 / 首屏即相机 / 拍照即完整案件"等旧规则。 |

---

## 1 产品概述

### 1.1 一句话定义
一款**先行动、再想目标**的破案式启动器。用户可以**直接拍照**开工：AI 侦探在一面黑暗的推理墙上识别突破口、只派发**一条** 2-5 分钟能做完的「启动线索」；用户做完后拍照交证据，前后对比、吊灯变亮、黑雾退散、印章盖章。获得第一次行动反馈后，系统邀请用户**委托正式案件目标**，AI 据此生成任务树，推理墙随每一步推进逐渐被照亮，直至整案结案、生成结案报告、归档。用户也可从首页**直接委托**目标明确的案件，跳过快速启动。

### 1.2 愿景
让每一个卡在"不知从哪开始"的人，先迈出第一步，再想清楚目标。我们不解决"如何让人坚持"，我们解决"如何让人此刻就开始"，并让整个行动过程像一面被一盏盏点亮的推理墙——**从未知走向清晰**。

### 1.3 核心价值主张
> **goblin.tools 帮你把任务拆小，开工侦探社帮你把乱摊子拍下来、破案式地一步步破掉，用前后照片证明你赢了，并把全过程挂在一面逐渐亮起的推理墙上。**

- 把"拆解"从**文字**迁移到**真实视觉场景 + 用户提交的佐证材料**（补 goblin.tools"仅文字、脱离场景"短板）；
- 把"验证"从**孤立聊天证据**升级为**前后对比破案叙事**（补 Shelpful"只事后单张验证"短板）；
- 把"游戏化奖励"从**抽象虚拟象征**绑死到**真实场景变化 + 推理墙渐进揭示**（补 Forest/Finch/Habitica"奖励与现实脱钩"短板）；
- 把"启动"从"先想清楚目标"反转成"先做一个动作、再补目标"（补所有待办工具"逼用户先规划"的致命短板）。

### 1.4 Slogan
- 主：**你不是不想做，只是还没找到第一条线索。**
- 副：**拍一张，就破案。先动手，再破案。**
- 求救口吻：**这案子有戏，先动这一步。**

---

## 2 背景与问题

### 2.1 核心洞察：拖延的瓶颈是"启动"，不是"坚持"
执行功能研究将**任务启动（task initiation）**定义为独立的执行功能子项。该环节受损时，用户卡在"不知从哪开始"的瘫痪感，常被误标为"懒惰/拖延/逃避"——尤其 ADHD 成人，启动所需认知能量显著高于神经典型者（ADDA；Kofler et al., 2024, PMC）。神经基础是**多巴胺调节异常 + 前额叶执行功能不足**，解释了"再努力一点"式劝导为何无效。

### 2.2 加剧启动摩擦的两个外部因素
1. **选择过多（choice overload）→ 决策瘫痪**。Iyengar & Lepper (2000) 果酱实验：24 种比 6 种停留更多（~60% vs ~40%），但**购买率从 ~30% 暴跌到 ~3%**。对应桌面：先收垃圾、先找资料、还是先开电脑？选项越多越动不了。
2. **羞耻/说教 → 情绪回避循环**。拖延与羞耻显著正相关；羞耻触发回避，回避强化羞耻（Sirois et al.）。"你怎么还没做""加油坚持"恰好命中拖延者最脆弱点。

### 2.3 降低启动摩擦的循证杠杆

| 干预 | 机理 | 证据 |
|---|---|---|
| 收敛到"只给一个动作" | 结构性移除决策环节 | Iyengar & Lepper (2000) 果酱实验 |
| 如果-那么计划（implementation intentions） | 把启动控制权外包给环境线索 | Gollwitzer & Sheeran (2006) 元分析 94 项，d≈0.65 |
| 微步骤 + 5 分钟承诺 | 行为激活：行动先于动机；蔡加尼克效应 | Behavioral Activation (CBT)；Zeigarnik effect |
| **先行动、再定义目标** | 用已完成的小动作打破"必须先规划"的瘫痪 | 行为激活"动机跟在行动之后"；本作双入口设计的心理学依据 |
| 自我原谅 | 打破"愧疚→回避"循环 | Pychyl：原谅自己拖延的学生下次拖延更少 |
| 视觉/图片提示优于文字清单 | 执行功能起于非语言工作记忆 | GrowNOW ADHD / Teaching Forward |

### 2.4 目标场景（MVP 收敛为单一物理场景）
物理可见、可拍照、且"乱到让人瘫痪"的**桌面**现场：大学宿舍书桌（外卖盒 + 书本 + 数据线）、堆积的作业/资料。共同特征：选项多、视觉混乱、每一步都不难但迈不出去。

> **非桌面场景（书包/房间/电脑屏幕）在 V2.0 前不进入 MVP**，因为闭环机制与桌面不同（详见 §7.5 场景可行性分级）。**主动委托模式**虽不依赖桌面照片，但其正式任务的证据验证仍优先走"可拍照交差"的高可验证路径（详见 §7.6）。

---

## 3 目标用户与画像

### Persona 1：拖延大学生 小林（核心种子）
- 画像：大三，期末周。书桌上外卖盒叠三个、课本数据线缠一起、论文还开着空白文档。
- 痛点：知道"该整理"，但眼前 5 个可选动作，决策成本太高，最后刷 2 小时手机；"保研作品集"这种大目标更是连想都不敢想。
- 过往：滴答清单列了 20 条待办，越看越焦虑关掉。
- 期待：别让我做选择、别让我先填一大堆计划，告诉我现在动哪一只手。
- 成功标准：拍一张照片，3 分钟后桌上少了一个外卖盒；行动起来后顺势把"作品集信息架构"委托成一个正式案件。

### Persona 2：ADHD 倾向者 阿哲（付费意愿种子）
- 画像：25 岁，设计师，确诊 ADHD。任务启动是日常最大障碍，文字清单对他"基本失效"。
- 痛点：goblin.tools 拆完仍不知道"手该往哪放"；Forest 树死了反而更自责。
- 期待：低评判、可重启、视觉锚点明确、能证明"我真做了一点"。
- 成功标准：连续 3 天每天破一案，没有一天因"断签"弃用。

### Persona 3：被复杂目标压住的人 阿May（主动委托模式典型用户）
- 画像：研究生，要"完成保研作品集信息架构"，资料堆成山。
- 痛点：目标很清楚，但太大、太抽象，"开始"像一堵墙。
- 期待：我把目标和材料交给 AI，它帮我把大目标拆成第一步。
- 成功标准：从首页"新建案件"委托目标 → AI 生成任务一"整理现有作品清单"→ 迈出第一步。

> **定位**：ADHD / 神经多样性人群是核心种子用户之一（与 goblin.tools/Tiimo/Shelpful 同赛道，付费与留存意愿已被验证），但产品面向**所有被启动摩擦卡住的普通人**。

---

## 4 竞品分析与差异化

### 4.1 竞品局限速查表

| 竞品 | 定位 | 核心局限（对本作的缺口） |
|---|---|---|
| **goblin.tools / Magic ToDo** | AI 文字任务拆解（递归 chunking + spiciness 滑块） | 纯文字、**不能处理照片/视觉内容**、无完成验证、**拆完仍不知道现实中从哪下手**；要求用户先输入目标（对启动障碍者已是门槛） |
| **Shelpful** | AI 问责伙伴 + 拍照验证完成度 | **仅事后单张验证**、无拆解、无前后对比、无叙事 |
| **Tiimo** | ADHD 可视化日程（计时器/时间块） | 日程规划导向、**不采集真实物理场景**、无叙事、无完成验证 |
| **Forest** | 专注种树（损失厌恶） | 游戏化奖励是**抽象虚拟象征**，与现实任务脱钩；树死了制造内疚 |
| **Finch** | 虚拟宠物自关怀 | 奖励虚拟；优点"停一周无负反馈"（本作借鉴） |
| **Habitica** | RPG 化任务 | 抽象 RPG 数值、红黄惩罚、**脆断 Streak**、对普通用户偏复杂 |
| **番茄Todo / 滴答清单** | 专注计时 / GTD 全流程 | 纯数字清单、无拍照、无视觉场景、无叙事 |
| **Shelver / NeatHub（物屿）** | AI 拍照识别家庭收纳 | 聚焦"物品在哪/怎么收纳"，**非"乱摊子任务破案推进"** |

### 4.2 独占生态位（竞品四象限矩阵）

```
                  完成验证（有事中/事后验证）
                          ▲
        Shelpful ●        │        ★ 开工侦探社（独占）
       （偏真实+          │   （真实物理场景 + 破案叙事 + 推理墙渐进照亮 +
         事后单张）        │     前后对比验证 + 动态单步 + 双入口）
                          │
   ───────────────────────┼───────────────────────► 真实物理场景 / 行动优先
   抽象                    │
   goblin.tools ●         │   Forest / Finch / Habitica ●
   滴答清单/番茄Todo ●     │   （抽象象征性奖励，无真实场景锚点）
   Tiimo ●（抽象时间）     │
                          ▼
                  无验证 / 象征性奖励
```

**全市场无直接同位对手**：开工侦探社是**唯一**把"先行动再定义目标（双入口）→ 破案叙事 → 推理墙渐进照亮 → 前后对比验证 → 动态单步拆解"闭环缝合的产品。

### 4.3 必须正面对标的两个及格线
1. **对标 goblin.tools 的拆解体验**：本作任务树生成（委托模式）必须达到 goblin.tools 的拆解质量——锚定可见/可执行/可递归。
2. **对标 Shelpful 的拍照验证**：必须做出**前后对比（同一视角 before/after）**独占差异，并把验证编织进"破案成功 + 推理墙揭示"叙事。

---

## 5 核心理念与设计原则

**一句话哲学**：默认用户此刻只有 10% 的电量。把每一步的能量成本压到接近 0；任何需要用户思考/选择/打字的环节，都视为设计缺陷——**除非用户自己准备好说目标**。

| 原则 | 含义 | 依据 |
|---|---|---|
| **P1 行动优先，相机为王** | 主页视觉中心是相机大按钮；用户可零输入、零规划直接拍照开工；正式目标可延后到行动之后 | 行为激活"动机跟在行动之后" |
| **P2 只给一步** | 任何时刻系统只输出"此刻能做的一个动作"，候补线索对用户不可见 | Iyengar & Lepper 果酱实验（24 选项购买率 ~3%） |
| **P3 双入口，殊途同墙** | 快速开工（拍照启动）与主动委托（先填目标）都汇入同一面推理墙 | 覆盖"不知目标"与"目标太大"两种启动障碍 |
| **P4 微步骤 + 5 分钟承诺** | 动作拆到极小，预估时长默认 5 分钟封顶 | 行为激活 + 蔡加尼克效应 |
| **P5 无限缩小救援** | 用户点"太难了"，任务持续分解到原子级，永不出现"放弃" | 任务启动障碍的可工程化杠杆 |
| **P6 破案不说教** | 禁用催促/责备/羞耻词；侦探叙事 + 低评判 + 可随时重启 | 羞耻触发回避循环（Sirois）；自我原谅减少拖延（Pychyl） |
| **P7 游戏化绑定真实证据 + 推理墙渐进照亮** | 奖励绑定真实场景变化与推理墙揭示，非抽象象征；真完成与信任制视觉区分 | 对标 Forest/Finch/Habitica 结构性差异 |
| **P8 反脆断** | 不押注"连续天数"，断签不归零，叫"案件搁置" | Habitica 第 4-6 周脆断 Streak 断崖 |
| **P9 沉浸式侦探世界** | 通过推理墙、相纸、吊灯、黑雾、档案袋、印章建立"侦探事务所"沉浸感，而非扁平卡片 | 视觉示意图定调；提升"破案"多巴胺与传播性 |

---

## 6 两种案件创建方式

产品存在两种不同的案件入口，必须在功能、流程、状态机中统一区分。

### 6.1 快速开工模式（quick_capture）
用户暂时不知道完整目标、或不愿输入文字时，**直接拍照**。系统根据拍摄的现实场景，识别一个最容易开始的突破口，并生成**第一条低门槛启动线索**。

这条启动线索的作用**不是完成整个案件**，而是：
- 帮助用户立即行动；
- 降低启动门槛；
- 让用户获得第一次完成反馈（盖章 + 第一枚线索徽章）；
- **在用户已经行动之后**，再邀请用户说明真正想完成的目标（弹出 E-1 委托案件）。

> 心理学依据（§2.3）：行为激活证明"动机跟在行动之后"。先让用户动起来，再让他定义目标，比逼他先规划有效得多。

### 6.2 主动委托模式（commissioned）
用户已经知道自己要完成什么时，从首页直接点击"新建案件"，进入 E-1 委托案件页：先填写**案件名称**和**佐证材料**，AI 根据案件目标和材料拆解任务树，并从任务一开始生成线索。

### 6.3 两者关系
- 两种方式最终都进入**同一个推理墙（E 案件界面）**，但前置流程不同。
- 首条启动线索（is_activation_clue=true）完成后，系统判断：若尚未填写正式目标 → 弹 E-1；若已有正式目标 → 继续下一条线索。
- 从首页直接进 E-1 时，没有已拍照片，推理墙第一张相纸暂时为空白；用户完成案件创建后 AI 生成任务一，完成任务一并验证后，第一张照片才正式显示在墙上并盖章。

---

## 7 产品功能（按 A–I 页面组织）

> 优先级：**P0** = 周末 MVP 必有；**P1** = 上线首版；**P2** = 路线图。每项标注 **[真跑/mock]**。
> 完整页面流转见 §8，状态机见 §9，信息架构见 §10，视觉规范见 §11。

### 7.1 A 启动页 / Logo 动画
- **描述**：打开 App 播放"开工侦探社"Logo 动画。视觉：黑暗环境 → 微弱灯光 → 胶片移动 → 放大镜扫过 → Logo 逐渐显现 → 印章落下。
- **跳转**：动画结束判断用户状态——新用户 → B 登录页；已登录老用户 → C 主页面。
- **验收**：动画 ≤ 2.5s，可点击跳过；尊重 prefers-reduced-motion（降级为 Logo 淡入 + 印章静态落）。
- **优先级 / 实现**：P0，**[Lottie 真跑，简化版]**。

### 7.2 B 登录页（仅首次新用户）
- **描述**：仅在新用户第一次使用时出现。包含：微信快捷登录、手机号登录、用户协议与隐私政策；**提供"游客体验/稍后登录"**，避免登录成为新的启动阻力。
- **关键规则（修正 v2.0）**：本版**新增登录页**，但登录必须**极低摩擦**——不要求填写复杂个人信息，不设多页引导。游客模式下功能正常，但档案/徽章不持久化，首次完成案件时再温和引导登录保存。
- **验收**：微信一键登录 ≤ 3s 完成；"游客体验"入口常驻可见；登录失败有侦探化文案兜底。
- **优先级 / 实现**：P0，**[真跑微信/手机登录 + 游客态]**。

> ⚠️ **v2.0 旧规则删除**：原"首次使用不登录 / 打开即相机"作废。新规则：A 启动 → B 登录（仅首次）→ C 主页面。

### 7.3 C 主页面（三按钮结构）
- **描述**：页面中央为最大的**相机**主按钮；相机按钮下方并排两个次级按钮——**左侧：新建案件**、**右侧：档案库**；页面右上角显示**用户头像**，点击进入 H 用户界面。
- **核心分支**：
  1. 点"相机"：C → D 拍照 → E 推理墙 → F 任务（快速开工模式）
  2. 点"新建案件"：C → E-1 委托案件 → E 推理墙（主动委托模式）
  3. 点"档案库"：C → G 档案库
  4. 点头像：C → H 用户界面 → I 设置
- **回访分叉**：回访且有未结案件时，主页顶部置一张"案件还在等你，继续？"卡片（点击精确回到上次离开状态）；无未结案件则展示三按钮。
- **验收**：视觉中心是相机按钮（居中且最大），"拍一下就开始"为视觉主行动；但"新建案件"委托入口与之**等价可达**，二者均通向同一推理墙（§6 双入口）；禁用复杂仪表盘或大量并列信息；首屏可见主操作 ≤ 3 个（相机/新建案件/档案库）。
- **优先级 / 实现**：P0，**[真跑]**。

> ⚠️ **v2.0 旧规则删除**：原"首页全屏相机预览 / 首页只有相机"作废。

### 7.4 D 拍照页面 + 定格转场
- **描述**：D 拍照页采用全屏取景界面（仅本拍照页全屏取景，非首页）；屏幕四角出现取景边框；页面下方中央为拍照按钮；保留相册上传入口；提供返回和闪光灯等必要功能但不喧宾夺主。
- **拍照后转场动画（共 5 步，必须写清）**：
  1. 四角取景框内的内容被截取并定格反馈；
  2. 图片缩小到屏幕中央；
  3. 生成相纸边框；
  4. 相纸向上移动；
  5. 进入 E 案件界面。
- **本地质量前置检测**（<200ms）：过暗/严重模糊 → "现场太暗了，侦探看不清，调一下光再拍？" + 重拍按钮，避免烂图喂给 VLM。
- **相机降级**：权限被拒 → 侦探化引导 + 直跳系统设置深链；返回后自动续拍；持续拒绝走相册；MVP 可设"示例桌面"开关兜底。
- **验收**：点快门 ≤ 3s；转场动画 ≤ 1.5s 且可被 prefers-reduced-motion 降级；权限授予后自动续拍，不点第二次快门。
- **优先级 / 实现**：P0，**[真跑拍照 + mock 示例桌面兜底]**。

### 7.5 E 案件界面 / 推理墙（核心页面）
这是整个产品最重要的核心页面。

**页面视觉**：
- 整体环境是一面**黑暗的推理墙**。
- 页面上方中央有一盏**微弱的吊灯**，只照亮当前照片，其他区域被黑暗和**黑雾**笼罩。
- 用户刚才拍摄的图片：生成**相纸边框** → 用夹子夹在绳子上 → 绳子横向穿过画面 → 相纸轻微摆动 → 相纸与墙面之间存在阴影和空间层次。
- 页面左上角显示"案件面板"；右上角显示"返回"。
- 支持：双指放大缩小、单指拖动、点击照片聚焦、默认镜头以当前一张照片为视觉中心、随案件推进逐渐展示更大的推理墙。

**AI 识别状态（按模式分支）**：
- **quick_capture 模式**（有拍照）：进入页面后 Agent 识别照片。识别过程中：放大镜在照片上左右循环移动、灯光轻微晃动、显示"侦探正在寻找突破口"等动态文案、保留三档 loading 与异常降级（§12.7）。
- **commissioned 模式**（第一张相纸 blank、无照片）：**不走"识别照片"**；显示"等待任务一"占位 + §12.7 loading"正在拆解案件"，AI 基于案件名称+材料生成任务一直接填入线索卡；放大镜扫描动画**仅在 quick_capture 有照片时触发**。
- 两种模式识别/生成完成后，页面下方均出现**线索卡**：任务编号（如"任务一"）、任务标题、任务详细说明、预计完成时间、**确认线索**按钮、**求救入口**。
- 点"确认线索"进入 F 任务界面。

**推理墙渐进照亮（与任务推进绑定）**：
- 第一阶段：只有当前照片被微弱灯光照亮，大部分画面处于黑暗黑雾，当前照片为视觉中心。
- **commissioned 模式第一张相纸特殊规则**：进入 E 时第一张相纸=blank（无照片），完成任务一并验证后才显示为证据照片+盖章；其后再按下方通用规则推进（与 §6.3 / §8.3 一致）。
- 每完成一条任务并验证：空白相纸显示为对应证据照片/任务卡 → 盖章动画 → 获得徽章 → 灯光闪烁 → 黑雾消散一部分 → 镜头横移/缩放 → 新的空白相纸和线索区域出现。
- 全部任务完成：所有黑雾消散、灯光变明亮、镜头拉远、展示完整推理墙（多张相纸、档案材料、绳子、关联线形成错综复杂构图）、所有印章徽章展示 → 触发整案结案动画 → 生成结案报告 → 案件进入档案库。

**验收**：
1. 圈框由 Qwen2.5-VL 单次调用产出（含 bbox + 任务文案）；坐标按 resized 基准换算（§14.1）；
2. 任意时刻只展示**当前一条线索**，候补对用户不可见；
3. 任务文案锚定照片中可见、可触及的具体物体/区域；
4. MVP 推理墙实现 2-3 张相纸的单绳悬挂 + 黑雾透明度变化；复杂关联动画列后续版本（§17）；
5. 灯光/黑雾/相纸摆动均尊重 prefers-reduced-motion。

**优先级 / 实现**：P0，**[真跑 Qwen2.5-VL 单次调用 + 像素 diff 验证；推理墙 MVP 简化版真跑，复杂动画 mock/后续]**。

### 7.6 E-1 委托案件 / 新建案件页
- **页面**：上方中央显示"委托案件"。
- **字段**：
  1. **案件名称**（格式示例："案件名称：完成保研作品集信息架构"）——代表用户真正希望解决的目标。
  2. **佐证材料**：支持图片、文件、文档、截图、拍照、文字补充。用于帮 Agent 理解任务背景、现状、限制条件和最终交付目标。**允许不上传直接继续**，但提示"材料越具体，侦探越容易找到准确线索。"
- **按钮**：下方"**确认委托**" + "**归档**"。
  - 点"确认委托"：① AI 读取案件名称和佐证材料 → ② case_status 进入 `analyzing`（显示 §12.7 loading"正在拆解案件"）→ ③ 分析案件目标并生成任务树 → ④ case_status 切 `active`，只向用户展示当前第一条正式线索 → ⑤ 返回 E 推理墙。
  - **方式二（首页新建）的相纸规则**：确认委托后，E 推理墙第一张相纸保持 `blank`（空白）状态，AI 生成任务一并展示；用户完成任务一并拍照/上传验证通过后，第一张相纸才从 blank 替换为正式证据照片并盖章（与 §6.3 / §8.3 一致）。
  - 点"归档"：保存当前快速启动成果 → 作为已完成/暂存案件放入 G 档案库 → 跳转 G。
- **两种进入方式**：
  - 方式一：从相机开始，完成快速开工第一条启动线索后，系统自动弹出 E-1。
  - 方式二：C 主页面点"新建案件"，直接进入 E-1（此时无已拍照片，推理墙第一张相纸空白；完成案件创建 AI 生成任务一；完成任务一并拍照/上传验证后，第一张照片才正式显示在墙上并盖章）。
- **验收**：案件名称必填、佐证材料可选；任务树生成 TTFR ≤ 5s（文本拆解，比视觉快）；拆解锚定材料、可执行、可递归（质量阀门 §15）；允许跳过材料但给提示。
- **优先级 / 实现**：P0，**[真跑 LLM 任务树拆解 + 质量阀门]**。

### 7.7 F 任务界面
- **页面内容**：当前任务名称、当前任务说明、**可爱的侦探角色**、侦探使用放大镜调查的**循环动画**、当前案件/任务的简化状态、求救信号入口、页面下方"**完成任务**"按钮。
- **侦探动画语义**：这里的动画**不是**表示系统仍在生成任务，而是表示——任务正在进行、侦探正在等待用户带回证据、当前线索仍处于调查状态。
- **点"完成任务"后分两种情况**：
  - **快速开工模式的启动线索（is_activation_clue=true）**：可采用**轻量完成确认**（无需严格拍照验证）。完成后严格按以下时序执行（**禁止并发**）：①轻量完成确认 → ②返回 E 推理墙 → ③**完整播放盖章动画**（落下的是「启动章」——区别于金/银章；墙面轻微震动、照片与墙面接触区域出现盖章、获得「启动徽章」、显示"线索一完成"）→ ④**盖章动画播完后**才弹出 E-1 委托案件。
  - **正式案件的后续线索**：需拍照验证 / 上传图片 / 上传文件 / 或按任务类型提供其他佐证材料；完成验证后才盖章。无法通过视觉验证的任务，继续走 §7.9 验证分级与信任制。
- **验收**：侦探动画循环流畅；求救入口常驻（§12.3）；完成后正确分流（启动线索→轻量确认→E-1；正式线索→证据验证→盖章）。
- **优先级 / 实现**：P0，**[真跑分流逻辑 + Lottie 侦探动画]**。

### 7.8 G 档案库
- **描述**：使用"案件文件夹"表达不同案件。每个案件表现为：档案文件夹/档案袋、文件标签、案件名称、当前状态、完成线索数量、最近更新时间、完成印章或冷案标记。文件夹横向或纵向堆叠，用户手指滑动浏览。
- **点击文件夹**：
  - 进行中案件 → 进入对应 E 推理墙；
  - 已完成案件 → 进入完整推理墙或结案报告；
  - 搁置案件（cold case）→ 进入恢复案件页面，保留原进度（§7.11）。
- **验收**：文件夹信息齐全、滑动流畅；点击后按状态正确跳转；保留 cold case 恢复机制。
- **优先级 / 实现**：P0，**[真跑案件状态保存与恢复；文件夹视觉简化版]**。

> ⚠️ **v2.0 旧规则删除**：原"档案库仅使用普通列表卡片"作废，改为档案文件夹/档案袋堆叠。

### 7.9 任务可验证性分级（保留 v2.0，防信任制空心化）

| 级别 | 判定 | 验证方式 | 印章 / XP |
|---|---|---|---|
| **高可验证** | 移走杯子、收外卖盒（视觉证据强） | 像素 diff + VLM 旁白主判 | 金章 + 满额 XP |
| **低可验证** | 读完一章、写完代码、整理信息架构 | 信任制，但需附文字/时长/文件佐证 | 银章 + XP ×0.7 |
| **不可验证** | 纯思考型任务 | 不立案，或转"思考型案件"独立流程（无前后对比，纯番茄钟式） | 单独 XP 池 |
| **启动线索** | 快速开工第一条（is_activation_clue） | 轻量完成确认（无需严格验证） | 启动徽章 + 固定 XP（不污染北极星） |

- **防刷**：信任制连续触发 N=3 次给软提示"要不要换一桩能拍照交差的？"；
- **图片去重**：after 图 pHash 与 before/历史雷同 → "好像和上次一样，真的有变化吗？"（加摩擦不强制拦截）。

### 7.10 场景可行性分级（MVP 只做桌面）

| 场景 | MVP 处置 | 闭环可行性 | V2.0 验证方案 |
|---|---|---|---|
| **桌面** | **MVP 唯一拍照场景，写死** | 高 | — |
| 书包 | 推 V2.0 | 低 | "掏出来摆好再拍"流程 |
| 房间 | 推 V2.0 | 低 | "局部拍照多次拼图" |
| 电脑屏幕 | 推 V2.0 | 闭环断裂 | "信任制为主 + 窗口/标签页清单" |
| **纯目标型（委托模式，无物理现场）** | **MVP 支持，走信任制/文件佐证** | 中 | 任务树文本拆解 + 文件证据 |

### 7.11 cold case 激活流程（保留 v2.0）
- 档案页 cold case 可点"重启此案"；重启后恢复原任务树和推理墙进度，或"从头来"（用户选）；重启案给 ×1.3 XP 加成。
- 案件级 cold case 与用户级热度是两回事：单条线索 cold case 后，用户可选"跳过该线索开下一条"或"整案转 cold case"，不被单案卡死；用户随时可"搁置此案开新案"。

### 7.12 H 用户界面 / I 设置界面（保留 v2.0）
- **H 用户界面**：用户头像、名称、完成案件数量、获得徽章数量、证物收藏、个人信息、成就、消息、帮助与反馈、设置入口。
- **I 设置界面**：账号与安全、隐私设置、通知设置、无障碍、减弱动效、单手模式、深色模式、休假模式、数据管理、清除数据、关于产品、退出登录。

---

## 8 完整用户流程

### 8.1 页面流转总览
```
A 启动页 ──(新用户)──► B 登录页 ──► C 主页面
          └─(老用户)────────────────► C 主页面

C 主页面 ──相机──► D 拍照页 ──定格转场──► E 推理墙 ──确认线索──► F 任务界面
        └─新建案件──► E-1 委托案件 ──确认委托──► E 推理墙
        └─档案库────► G 档案库 ──►(进行中)E / (已完成)推理墙·结案报告 / (搁置)恢复页
        └─头像──────► H 用户界面 ──► I 设置界面

F 任务界面 ──完成任务──►
   ├─ 启动线索(轻量确认) ──► E 盖章动画 ──► E-1 委托案件(填正式目标)
   └─ 正式线索(证据验证) ──► E 盖章+黑雾退散+新相纸 ──► 下一条线索 / 整案结案
```

### 8.2 快速开工模式主流程
```
C 点相机 → D 拍照（定格→缩小→相纸边框→上移）→ E 推理墙
  → AI 识别（放大镜扫描+吊灯晃动+"侦探正在寻找突破口"）
  → 线索卡（任务一）→ 确认线索 → F 任务界面
  → 用户做掉启动线索 → 完成任务（轻量确认）
  → 回 E：印章落下+墙面震动+第一枚线索徽章+"线索一完成"
  → 弹出 E-1 委托案件
  → 用户填案件目标(+材料) → 确认委托
  → AI 生成任务树 → 回 E，只展示任务一（正式）
  → 循环：F 做正式线索 → 拍照/上传证据 → 验证 → E 盖章+黑雾退散+新空白相纸
  → 全部完成 → 整案结案动画 → 结案报告 → 进 G 档案库
```

### 8.3 主动委托模式主流程
```
C 点新建案件 → E-1 委托案件（case_status=draft，第一张相纸空白）
  → 填案件名称(+材料) → 确认委托
  → case_status=analyzing（显示 loading"正在拆解案件"）→ AI 分析目标 → 生成任务树
  → case_status=active → 回 E，展示任务一
  → F 做任务一 → 拍照/上传证据 → 验证成功 → 第一张相纸从 blank 正式显示+盖章
  → 循环至整案结案
```

### 8.4 中途退出 / 恢复
- 任何阶段退出 → 自动保存；回访主页顶部置"案件还在等你，继续？"卡，精确回到上次状态。
- 离开超 2h 提示"现场可能变了，重拍更新？/ 接着旧案子"双选项。
- TTL 到期（内存 10min）→ 恢复时提示"证据已过期，重拍一张"（非崩溃）；任务树元数据落本地 storage 兜底。

---

## 9 案件与线索状态机

### 9.1 案件创建方式
`creation_mode`（全文统一字段名，与 §14.7 数据模型一致）：
- `quick_capture`：拍照快速启动；
- `commissioned`：主动委托案件。

### 9.2 案件状态
`case_status`：
- `draft`：commissioned 模式正在 E-1 填写委托信息（仅主动委托模式经过此态）；
- `analyzing`：AI 正在分析/生成任务树（commissioned 确认委托后；或 quick_capture 提交正式目标后）；
- `active`：案件进行中——**quick_capture 模式拍照立案后即进入 active（此时 has_formal_goal=false，仅启动线索）**，提交正式目标并生成任务树后仍为 active；
- `waiting_evidence`：等待证据；
- `cold_case`：案件搁置；
- `archived`：用户主动归档；
- `completed`：整案完成。

### 9.3 线索状态
`clue_status`：
- `blank`：推理墙上的空白照片（尚未生成/尚未对应任务）；
- `generating`：正在生成；
- `pending`：等待用户确认线索；
- `in_progress`：任务进行中（F 页）；
- `waiting_evidence`：等待上传证据；
- `verifying`：正在验证；
- `done`：完成并盖章；
- `suspended`：暂时搁置。

### 9.4 首条启动线索标记
新增字段 `is_activation_clue`（boolean），区分：
- 快速拍照后生成的**低门槛启动线索**（轻量确认，不污染北极星）；
- 用户输入正式目标后生成的**正式任务线索**（需证据验证）。

**分流规则**：首条启动线索完成后判断——
- 若尚未填写正式案件目标 → 弹出 E-1；
- 若案件已存在正式目标 → 继续下一条线索。

### 9.5 状态流转图
```
【commissioned 主动委托】draft → analyzing → active
【quick_capture 快速开工】(拍照立案)→ active[has_formal_goal=false]
                              →(启动线索完成→弹E-1→确认委托)→ analyzing → active[has_formal_goal=true]

案件 active ⇄ waiting_evidence
   ├─(用户搁置)─► cold_case ──(重启)──► active
   ├─(用户归档)─► archived
   └─(全部线索 done)─► completed

线索：blank → generating → pending →(确认)→ in_progress →(完成任务)→ waiting_evidence
                                                          →(验证通过)→ done(金章)
                                                          →(验证未过/补拍)→ waiting_evidence
                                                          →(信任制)→ done(银章)
                        in_progress ──(太难了/搁置)──► suspended
```

---

## 10 信息架构

```
开工侦探社
├── A 启动页
├── B 登录页
├── C 主页面
│   ├── 相机
│   ├── 新建案件
│   ├── 档案库
│   └── 用户头像
├── D 拍照页面
│   ├── 相机取景
│   ├── 拍照
│   ├── 相册上传
│   └── 照片定格转场
├── E 案件界面 / 推理墙
│   ├── 案件照片（相纸+夹子+绳子）
│   ├── 放大镜识别动画
│   ├── 当前任务信息（线索卡）
│   ├── 确认线索
│   ├── 求救信号
│   ├── 图片缩放与拖动
│   ├── 证据验证
│   ├── 盖章反馈
│   ├── 黑雾消散
│   └── 完整推理墙
├── E-1 委托案件 / 新建案件
│   ├── 案件名称
│   ├── 佐证材料
│   ├── 确认委托
│   └── 归档
├── F 任务界面
│   ├── 当前任务
│   ├── 侦探动画
│   ├── 求救信号
│   ├── 完成任务
│   └── 拍照或上传证据
├── G 档案库
│   ├── 案件文件夹
│   ├── 滑动浏览
│   ├── 进行中案件
│   ├── 已完成案件
│   └── cold case
├── H 用户界面
└── I 设置界面
```

---

## 11 视觉风格与美术系统（新增章节）

### 11.1 整体风格定义
一款具有艺术表现力的**侦探推理主题平面 2D 插画应用**。画面采用**黑红高对比配色**，以黑暗、黑雾和低饱和冷色表达"任务尚未被解决"，以暖红、橙色和米白色灯光表达"线索发现、行动推进、案件完成"。

它**不是**写实刑侦界面，**也不是**传统效率软件的扁平卡片界面，而是通过**推理墙、相纸、吊灯、档案袋、印章**建立具有沉浸感的"侦探事务所"世界。

### 11.2 色彩系统

| 角色 | 色值 | 用途 |
|---|---|---|
| 背景深黑 | `#1A1A1A` | 推理墙背景、主环境 |
| 黑红 / 暗酒红 | `#8B0000` / `#C41E3A` | 强调色、按钮、印章、边框 |
| 深棕 | `#3E2A20` | 档案袋、皮革质感 |
| 棕褐 / sepia | `#D2B48C` | 老旧相纸、照片底色 |
| 暖米白 / 灯光黄 | `#F4E4C1` | 相纸边框、吊灯光晕底色 |
| 灯光橙（强调） | `#E8A04C` | 吊灯光晕主色、行动推进色 |
| 灰 | `#4A4A4A` / `#6E6E6E` | 次要文字、未激活 UI |
| 盖章红（强调） | `#C41E3A` | 印章主色（与暗酒红同值，兼作强调） |
| 徽章金（强调） | `#D4AF37` | 金章 / 证据验证完成 |
| 信任制银（强调） | `#C0C0C0` | 银章 / 信任制完成 |

**铁律**：未完成状态**不使用刺眼警示红**；黑色与黑雾表达"未知"，暖色灯光表达"被发现/被完成"。设计先做黑白稿，确保对比度满足无障碍（正文/按钮 ≥ AA 4.5:1，关键操作 ≥ AAA）。

### 11.3 核心视觉元素

**1. 相纸**：承载现场照片、证据、任务结果；白/米黄纸质边框；可带磨损、折痕、污渍、手写编号；用夹子悬挂在绳子上；未完成任务时显示空白/模糊相纸；完成后替换为证据内容并盖章。

**2. 吊灯**：案件页面视觉焦点；初始灯光微弱、只照亮当前一张照片；案件推进时灯光闪烁；完成度提升后照亮范围扩大；全部完成后灯光完全明亮。

**3. 推理墙**：案件的核心空间载体；展示照片、文件、标签、绳子和关联关系；支持放大/缩小/拖动；默认聚焦当前任务照片；随推进逐步扩展；完成后呈现完整复杂的照片墙。MVP 只做单绳 2-3 张相纸 + 黑雾透明度，复杂关联线/物理摆动列后续。

**4. 档案袋与文件夹**：用于档案库和案件资料；每个案件对应一个文件夹/档案袋；用标签、编号、日期、印章表达状态；堆叠形成可滑动浏览界面。

**5. 绳子与夹子**：绳子串联任务发展过程；夹子固定照片和材料；新任务出现时新相纸挂到绳子上；轻微摆动增加生命力；**不要让绳子过于复杂**以免干扰文字阅读。

**6. 放大镜**：表达 AI 识别；在照片上左右扫描；点击/长按照片触发局部放大；同时是侦探角色的视觉符号。

**7. 印章**：表示单条线索完成；落下时伴随缩放、震动、声音、纸张反馈；保留金章（证据验证）/银章（信任制）区分；启动线索用"启动徽章"。

**8. 黑雾**：表示尚未明确的任务和未知区域；**不是失败或威胁提示**；每完成一条任务消散一部分；整案完成后完全消失；对"减弱动效"用户改为透明度渐变。

**9. 徽章**：每完成一条任务获得对应徽章；徽章与现实任务结果绑定；可展示在线索照片下方、结案报告、用户档案；避免只形成抽象数值奖励。

### 11.4 动效原则
- 所有循环动效（吊灯晃动、相纸摆动、放大镜扫描、侦探呼吸）尊重 `prefers-reduced-motion`，降级为静态/淡入。
- 关键反馈动效（盖章、黑雾退散、镜头移动）在减弱动效模式下改为透明度/位置瞬时变化 + 淡入。
- Canvas/合成特效（迷雾 destination-out、放大镜）在中端安卓目标机型 Demo 前压测 100 次帧率。

### 11.5 字体
标题/Logo 用衬线体（侦探卷宗感），正文用无障碍无衬线体（最小 15px，动态字体），白色为主、深底高对比。

---

## 12 交互与 UX 设计要点

### 12.1 关键微交互节奏（含转场）

| 阶段 | 状态 | 交互 | 容错 |
|---|---|---|---|
| D 拍照 | live 取景 | 点快门→定格→缩小→相纸边框→上移→E；权限授予后自动续拍 | 过暗/模糊前置拦截；权限被拒走降级/相册/示例桌面 |
| E 识别中 | 放大镜扫描+吊灯晃动 | 动态文案"侦探正在寻找突破口"+ 三档 loading | 15s 超时+降级+预设 case |
| E 线索卡 | 底部弹出唯一线索卡 | 点"确认线索"→F | 空状态三类（§7.5） |
| F 执行 | 侦探调查循环动画 | 点"完成任务" | 启动线索轻量确认；正式线索走证据 |
| F→E 完成 | 盖章+墙面震动+徽章 | 自动 | 未通过→补拍/信任制 |
| E 渐进 | 黑雾退散+灯光闪烁+新相纸 | 自动 | 减弱动效改透明度渐变 |
| E 整案结案 | 镜头拉远+完整推理墙+结案报告 | 自动 + 用户确认 | — |

### 12.2 文案系统（语气规范 + 禁用词 + 羞耻度测试）
**语气基调**：称用户"侦探/你"，任务=案件/线索，App 自称"事务所/总部"，完成=破案/结案，未完成=案件挂起（非失败）。温暖、自嘲、像朋友而非教练。

**禁用词清单**（一律不得出现）：
> 你应该 / 你必须 / 你又拖延了 / 怎么还没开始 / 加油 / 坚持 / 自律 / 效率 / 时间管理 / 别偷懒 / 你的连续记录已清零

**替换库**：

| 禁用 | 替换 |
|---|---|
| 坚持 | 侦探也是人 |
| 加油 | 这案子有戏 |
| 别拖延 | 现场我帮你守着 |
| 你怎么还没做 | 案子还在等你 |
| 断签 | 案件搁置（cold case） |
| 这次信你（单调） | 变量轮换：这次信你 / 交给你了 / 你说了算 |

**羞耻度测试（可执行）**：找 5 名含 ADHD 倾向用户朗读全部文案；任何让其中 ≥1 人"脸红/不适"的表述即重写；信任制文案单案件内变量轮换，避免"又信你"讽刺感。

### 12.3 求救按钮可见性与视觉规范
- **位置**：D 拍照屏（快门左侧"我拍不动"）、E 线索卡（抽屉底部）、F 任务界面（完成任务旁）、**领单确认后执行前**的真空期都要可达。
- **视觉**：低饱和"瘫坐小人/月亮"图标 + 文字（纯文字易被 ADHD 忽略）；触控区 ≥48dp（热区 56dp）；按压给"被接住"反馈。
- **降级三选项**：①把这单拆更小（无限缩小）②今天先记录不算失败（转 cold case + 落「留档章」，**不发银章**，不计 XP、不污染北极星）③就坐一会侦探陪你（30s 呼吸动画，结束给三出口：试着动一下/今天就到这/回首页，**不允许死胡同**）。永不"放弃"，永不扣分。

### 12.4 容错矩阵

| 异常场景 | 处理 | 文案 |
|---|---|---|
| 识别错——圈错物体 | 重新圈→重跑 VLM | "看走眼了，你来纠正" |
| 识别错——任务名不对 | 直接改文字→不重跑 | "看走眼了，你来纠正" |
| 任务无法验证 | 按 §7.9 分级处理 | "这次信你，侦探" |
| 中途退出 | 自动保存，主页置顶恢复卡 | "案件还在等你" |
| 非桌面场景 | 引导重拍桌面或走信任制/委托模式 | "这是哪种现场？" |
| VLM/任务树超时 | 15s 超时 + 降级 + ≥3 条预设 case | "侦探迷路了，换个案子" |
| API 限流 | 双 provider 切换 | 静默重试 |
| 委托材料解析失败 | 让用户补一句目标说明 | "再说细一点，侦探好下手" |

### 12.5 无障碍（WCAG 2.1 AA，关键操作 AAA）
1. 动效降级：尊重 prefers-reduced-motion；App 内"减弱动效"开关。
2. 可读性：正文最小 15px（动态字体），对比度 ≥ AA，关键 ≥ AAA。
3. 色彩非唯一载体：状态必有图标+文字。
4. 触达区：相机/求救/完成/确认线索 ≥48×48dp，分布在屏幕下 60% 拇指区；左/右手单手模式。
5. 屏幕阅读器：所有交互元素带 accessibilityLabel；盖章成功公告"案件已结"。
6. Canvas 特效无障碍：放大镜触屏改长按预览；迷雾揭示区带隐藏 aria-label"已探查：左上角区域"；提供"纯文字模式"开关（视觉游戏化降级为文字线索列表）。

### 12.6 认知负荷
单屏信息密度：C 主页 ≤3 主操作；E 推理墙 ≤当前 1 条线索可见；禁仪表盘并列。主色板 3 色内 + 功能色 3 类（进行中主色/完成盖章金银/求救灰蓝）。深色模式优先。

### 12.7 统一 loading 三档
- **>800ms**：动效 + 侦探文案；
- **>4s**：阶段文案（E"正在找突破口" / F"正在比对证据" / E-1"正在拆解案件"）；
- **>8s**："侦探需要点时间，先放着，稍后提醒你" + 允许后台离开。

---

## 13 游戏化系统设计

### 13.1 四层进阶结构

| 层级 | 单元 | 说明 | 数值 |
|---|---|---|---|
| 案件 Case | 一次工作对应一个案件 | 主流 25-45min，深度案 60-90min（首案启动线索 2-5min 属"启动"，不等同整案结案） | — |
| 线索 Clue | 案件内小里程碑 | 每案件 3-7 个 | 解锁 +10-15 XP |
| 证物章/徽章 | 跨案件可收集实体 | 有限枚举 | 首期 40-60 枚 |
| 侦探等级 | 1-50 级 | 解锁新特权/内容 | XP 曲线见下 |

> **印章 vs 徽章 口径（全文统一）**：
> - **印章**＝单次完成的墙面视觉反馈（不进收藏）：①**金章**＝证据验证完成；②**银章**＝信任制完成（低可验证任务，XP×0.7）；③**启动章**＝启动线索完成（区别于金/银，固定 XP，不污染北极星）；④**留档章**＝cold case 搁置安慰（不计 XP、不进收藏，§15）。
> - **徽章**＝跨案件可收集数据实体：**证物章**（金/银，进收藏册）＋**等级徽章**（见习侦探/转正考核等）。
> - **首案引导**给的是「启动章 + 见习侦探等级徽章」，**不是**证物章。MVP 枚举：证物章 2 枚（金/银）＋启动章＋见习侦探等级徽章。

### 13.2 XP 曲线与防刷分
- 公式：`XP_needed(L) = 80 × L^1.5`（1→2≈80；10→11≈2530；20→21≈7155）。
- 金章（真完成）满额 XP，银章（信任制）XP ×0.7，启动线索固定 XP（不污染北极星）。
- 禁批量刷分：同类任务连续第 4 次起 XP ×0.5；图片雷同（pHash）提示加摩擦。
- 每日软上限：单日 XP > 1200 后 ×0.5。

### 13.3 反脆断：Streak → "案件热度"
- 不叫"连续签到天数"，叫"案件热度"；完成 1 个有效案件 +1；中断**不归零**，进 cold case，每停摆 3 天 -1（最低 0）；回归首案 ×1.3 XP，显示"侦探回来了"；**彻底删除"连续记录已清零"文案**。

### 13.4 反 PUA 提醒（文案红线）
- 禁止"你今天还没开工""案件快过期了""别让线索溜走"（红色感叹号）；
- 允许"案发现场还留着你的记号，随时回来""上次你在这条线索卡住了，今天想换个角度吗？"；
- 每日 ≤1 条；"侦探休假模式"一键静音 7 天、热度不衰减、回归送"休假归来"纪念章。

### 13.5 不羞辱设计清单（必选项）
1. 无任何刺眼警示红用于未完成；
2. 断签/跳过永不归零、不扣分；
3. 失败/卡住案件可"转 cold case 归档"而非"失败"，且可重启；
4. 所有统计默认只和自己比；排行榜仅限自愿加入的"侦探公会"；
5. 绝不向通讯录/好友推送"某某已经破案了你呢"。

### 13.6 反沉迷 / 防第二焦虑
单日有效专注达 4h 触发"侦探下班"软提示；达 6h 强制冷却；每周"休庭日"默认关闭；工具复杂度分层（新手只看"开案-破案-结案"三步）。

### 13.7 冷启动与首周锚点（MVP 就上）
首案强制引导 3 分钟内破第一案 → 见习侦探徽章 + 首枚证物章；前 7 天每日首案 ×2 XP；第 7 天"转正考核"；第 10 天"破案 10 次纪念章"；第 14 天"热度保持奖励"。

---

## 14 技术架构与数据模型

### 14.1 VLM 选型与坐标基准（保留 v2.0 关键修正）
- **MVP 选型**：Qwen2.5-VL（DashScope）单次 structured output，一次产出 `{scene_type, bbox_2d:[x1,y1,x2,y2], task_title, task_hint, est_minutes}`。依据：bbox 回归上 Qwen2.5-VL ≈ Gemini，明显优于 GPT-4o/Claude。
- **坐标基准坑（must_fix）**：Qwen2.5-VL 输出是"相对 resized image 的绝对像素"。正确映射：`归一化比例 = resized 坐标 ÷ resized 尺寸 → × 前端显示尺寸`；必须记录模型实际 resized 尺寸；Demo 前打印 bbox 四值与原图叠合核对，列为周末硬验证项；错位时给"框歪了？手动调一下"入口。
- **任务树生成（委托模式）**：文本 LLM（可用 Qwen / Claude 文本模型）拆解，TTFR ≤ 5s；过质量阀门（锚定材料、可执行、可递归、禁泛泛步骤）。

### 14.2 前后对比验证（保留 v2.0：像素 diff 为主）
- MVP 用 OpenCV absdiff/SSIM 做像素 diff，零延迟、稳一个数量级；VLM 仅生成一句鼓励性破案旁白；最终通关权交给用户"确认完成"（信任制兜底）。依据：主流 VLM 对 subtle visual change 检测远低于人类，光照变化即误判。

### 14.3 Agent 循环延迟
- 单轮目标 P50：视觉 TTFR ≤3s，整轮 ≤10s；委托模式任务树 ≤5s。
- 四件套：静态前缀走 prompt 缓存（降延迟最高 85%）、图片压 1024px/JPEG q=80、全程流式、3 条预设 case 兜底。
- **跨 provider 不共享 KV cache** → 收敛单模型单次调用。

### 14.4 无限缩小救援（保留 v2.0）
MVP 走预设脚本树（每 case 5 层），不调 LLM；V1.1 上真 LLM decompose + 质量阀门。任务节点字段含 `status`、`failCount`、`decomposePrompt`、`verifiableLevel`、`isActivationClue`。

> **命名约定**：§14.4 驼峰（camelCase）为前端/内存模型别名；§14.7 蛇形（snake_case）为存储/接口权威字段名。二者一一对应（`fail_count`↔`failCount`、`verifiable_level`↔`verifiableLevel`、`is_activation_clue`↔`isActivationClue`），全链路以 snake_case 为准。

### 14.5 前端视觉特效
1. 迷雾：底层彩色照片 + 顶层灰雾 canvas，`destination-out` 挖洞；MVP 双层。
2. 吊灯/相纸摆动：Lottie 或 CSS keyframes；尊重 reduced-motion。
3. 盖章：PNG + CSS keyframes（scale 1.3→1 + 抖动）+ haptic。
4. 放大镜：CSS filter + overflow + transform 伪放大镜；触屏长按预览。
- 三者封装为 React 组件，**务必在中端安卓目标机型压测**，不要只在开发机验证。

### 14.6 隐私与持久化（保留 v2.0 五道闸）
- MVP：不落库，纯内存；图片内存/Redis TTL 10 分钟；任务树元数据（不含原图）落本地 storage 兜底；默认本地处理 + 手动涂抹敏感区 + 预录示例桌面；首次授权说明。
- V1.0：`user_progress`/`badge`/`case` 元数据脱敏持久化；原图仍不落库；一键清除全部数据。

### 14.7 数据模型（核心表，新增字段加粗）

| 表 | 关键字段 | MVP | V1.0 |
|---|---|---|---|
| `case` | case_id, user_id, **creation_mode**, **case_name**, scene_photo_ref, materials_ref[], fog_ratio, status, **has_formal_goal**, created_at, closed_at, **cold_case_since**(案件级搁置时间) | 内存 + 本地 storage | 持久化（脱敏） |
| `clue` | clue_id, case_id, parent_id, children[], title, hint, bbox, est_minutes, status(§9.3 八态), fail_count, verifiable_level, **is_activation_clue**, **suspended_since**(线索级搁置时间) | 内存 + 本地 storage | 持久化 |
| `evidence` | evidence_id, clue_id, before_ref, after_ref, diff_ref, vlm_judgment, user_confirmed, phash, **material_type**, **stamp_type**(gold/silver/activation/filed) | 内存 TTL 10min | 内存（原图不落库） |
| `badge` | badge_id, user_id, type, earned_at | 本地 storage | 持久化 |
| `user_progress` | user_id, level, xp, heat, cold_case_since, vacation_mode, last_active, **login_mode**(wechat/phone/guest) | 本地 storage | 持久化 |

### 14.8 周末 MVP 最小技术栈
- 前端：React18 + Vite + TypeScript（A–I 九页面，核心 E/F/E-1）；
- 后端：Next.js API Routes 或 Node + Fastify（`/analyze` 视觉、`/plan` 任务树、`/verify` 对比）；
- VLM：Qwen2.5-VL via DashScope 为主；任务树拆解可用 Qwen/Claude 文本；Claude/GPT-4o 仅离线评测；
- 部署：Vercel；图片临时存储：内存/Redis（TTL 10min）。

### 14.9 成本量化
单图 ≈2000 tokens；Qwen2.5-VL ≈0.008 元/千 tokens；单次视觉会话 ≈0.03-0.05 元；任务树文本拆解更便宜；日 100 会话月成本 ≈90-150 元；路演现场日成本可能破百元 → Demo key 设硬额度预警；Claude/GPT-4o 只做离线评测，**禁止进在线链路**。

---

## 15 「无限缩小救援」机制专项（保留 v2.0）

- MVP：预设每个 case 的 5 层缩小路径，点"太难了"沿树下降，不调 LLM。
- 示例：整理左侧区域(5min) → 拿走空杯子(2min) → 先拿起离你最近的那个奶茶杯(30s) → 把杯子提到离你 30cm 内(15s) → 用手碰一下那个杯子(5s)。
- 边界：递归深度 ≥5 次；每次更原子、更短（≤上一步 50%）、锚定可见可触及物体；缩到最小还不动 → "侦探陪你"呼吸动画 → 转 cold case + 落「留档章」（**不发银章**，避免把搁置误计为低质量完成；不计 XP、不计负面指标）；跨案连续 cold case 触发软干预。

---

## 16 关键指标与成功标准

### 16.1 北极星
**真实完成率**：从拍照/委托到通过 diff 验证或附了不同证据图的会话占比（启动线索轻量确认、信任制均不计入北极星，单独统计）。
- W1 北极星 A：真实完成率 ≥ 40%；
- W4 北极星 B：第 7 天"转正考核"达成率 ≥ 30%。

### 16.2 指标矩阵（节选）

| 层级 | 指标 | MVP | 上线 |
|---|---|---|---|
| 激活 | 首次启动→主页 ≤6s（含登录，仅首次）；回访 ≤3s | 100% | 100% |
| 激活 | 首案 5 分钟内拿到第一个徽章 | ≥90% | ≥95% |
| 双入口 | 快速开工 vs 主动委托 的真实完成率 | 分别统计 | — |
| 单次会话 | 真实完成率 | ≥40% | ≥50% |
| 委托转化 | 启动线索完成后填正式目标的转化率 | ≥50% | ≥70% |
| 求救 | "太难了"触发后仍完成案件比例 | ≥40% | ≥60% |
| 留存 | D1 / D7 / D14 | — | ≥40% / ≥30% / ≥20% |
| Demo | 路演后扫码注册率 | ≥30% | — |

### 16.3 反指标
信任制完成率 >30%（验证空心化）；单用户多案连续 cold case；单日 XP >1200 占比过高；求救后"放弃 App"比例 >50%。

---

## 17 MVP 范围 vs 未来路线图

### 17.1 MVP 必须实现（技术诚实，不追求全视觉效果）
- Logo 启动页；简化登录页（微信/手机 + 游客）；三按钮首页；拍照页 + 拍照缩小转场（5 步）；单张照片悬挂效果（相纸+夹子+绳子，**含简化摆动/阴影层次**）；简化吊灯效果；放大镜扫描动画；**简化版侦探调查循环动画（F 页，可用静态降级）**；当前任务卡；完成任务按钮；盖章动画（金/银/启动章）；**委托案件表单（案件名称 + 材料上传）**；2-3 张照片的推理墙扩展；黑雾透明度变化；档案文件夹基础页面；案件状态保存和恢复。
- **真跑**：Qwen2.5-VL 单次调用、像素 diff、委托任务树生成。

### 17.2 后续版本实现
- 大规模复杂推理墙（多绳、关联线、物理摆动）；高级绳索关联动画；完整物理摆动；多层动态灯光；全量徽章系统；高自由度推理墙编辑；多人协作案件；高级案件关系推理；真 LLM 无限缩小 decompose；自动预模糊。

### 17.3 防翻车 checklist（黑客松现场）
1. 所有 VLM/LLM 调用设 15s 超时 + 降级文案；
2. 至少 3 条"保证能演通"的预设 case 写死前端可一键触发；
3. API key 双 provider（主 DashScope + 备 OpenAI/Gemini）；
4. 现场用预录照片，消除光照/角度变量；
5. 验证步骤永远给用户"强制确认"出口；
6. 坐标基准对齐：Demo 前打印 bbox 四值与原图叠合核对；
7. 三特效在中端安卓各压测 100 次帧率；
8. 路演 key 硬额度预警。

---

## 18 风险与应对（节选矩阵）

| 风险 | 概率 | 影响 | 应对 |
|---|---|---|---|
| VLM 圈框坐标错位（resized 基准） | 高 | 高 | §14.1 坐标修正 + Demo 前叠合核对 + 手动修正入口 |
| 双入口增加认知负荷/开发量 | 中 | 中 | 主页视觉中心强压相机；E-1 表单极简；两入口共用 E/F 页面 |
| 登录页成为新启动阻力 | 中 | 高 | 微信一键 + 游客体验常驻；游客功能正常仅不持久化 |
| 委托模式任务树拆解质量不及 goblin.tools | 中 | 高 | 质量阀门 + 离线 A/B 对比 goblin.tools 输出 |
| 推理墙复杂动画中端机卡顿 | 高 | 中 | MVP 只做单绳 2-3 相纸 + 黑雾透明度；复杂动画列后续；目标机型压测 |
| 前后对比误判 | 高 | 中 | 像素 diff 替代 VLM 判定；用户信任制兜底 |
| 隐私（真实桌面含人脸/屏幕） | 中 | 高 | 五道闸；原图永不落库 |
| 第 3-4 周留存断崖 | 高 | 高 | 不押连续天数 + 案件热度不归零 + D7/D10/D14 锚点 |
| 被认知为"Shelpful 中文版" | 中 | 中 | 文案/定位强调"推理墙渐进照亮 + 双入口 + 前后对比破案" |

---

## 19 路演 Demo 脚本（分镜级，适配新流程）

**设定**：现场用预录"堆满外卖盒、书本、数据线的桌面"照片。默认走预设 case 保演示流畅，隐藏键切"真 VLM"演真圈框。

| 分镜 | 时长 | 画面 | 口播 |
|---|---|---|---|
| 1 | 0-4s | A 启动 Logo 动画（黑暗→灯光→放大镜→印章）→ B 一键登录 → C 三按钮主页 | "拖延最难的不是坚持，是启动。我们让用户先动手，再想目标。" |
| 2 | 4-10s | C 点相机 → D 拍照 → 定格缩小成相纸 → 上移 → E 黑暗推理墙 | "拍一张，就破案。" |
| 3 | 10-18s | E 吊灯只亮当前相纸，放大镜扫描 → 线索卡"任务一：先移走左上角两个空杯子" | "只给一条线索，不给整套焦虑清单。" |
| 4 | 18-26s | F 任务界面，侦探调查动画 → 用户点"太难了" → 缩成"先拿起最近的奶茶杯" | "觉得难？无限缩小，直到你能动。" |
| 5 | 26-36s | 完成 → 回 E：印章落下+墙面震动+第一枚徽章+"线索一完成" → 自动弹 E-1 | "动起来之后，再委托真正的目标——比如'完成保研作品集信息架构'。" |
| 6 | 36-48s | E-1 填案件名称(+材料) → 确认委托 → AI 生成任务树 → 回 E，黑雾退散、新相纸出现、镜头横移 | "整面推理墙，随每一步推进被一寸寸照亮。" |
| 7 | 48-60s | （隐藏键切真 VLM 真圈框）→ 整案结案：灯光全亮、镜头拉远、完整推理墙 → 结案报告 + slogan | "你不是不想做，只是还没找到第一条线索。开工侦探社。" |

**兜底**：任一分镜卡住 → 一键触发预设 case，保证演通。

---

## 20 待验证假设与开放问题

| 编号 | 假设 | 验证 | 失败标准 |
|---|---|---|---|
| H1 | 拍照比文字输入更降低启动门槛 | A/B 拍照 vs 文字入口真实完成率 | 拍照组未高 15pp+ |
| H2 | "只给一步"比清单完成率高 | A/B 单线索 vs 列表 | 单线索未更高 |
| H3 | 前后对比验证带来可证伪成就感 | 调研 + 复访率 | 无显著提升 |
| H4 | 无限缩小救援能救回卡死用户 | 求救后完成率 | <40% |
| H5 | 破案叙事比自律打卡更无羞耻 | 羞耻度测试 + 留存 | 未低于 Forest/滴答 |
| H6 | 案件热度 + D7/D10/D14 穿越第 3 周断崖 | D14/D28 留存 | D14 <20% |
| H7 | 游戏化绑定真实证据（金章）比抽象象征更有效 | 金/银/虚拟树留存对比 | 无差异 |
| **H8（新增）** | **"先行动再委托目标"比"先填目标"完成率更高** | A/B 快速开工 vs 主动委托的真实完成率与委托转化率 | 快速开工组未更优 |

**开放问题**：①粒度滑块是否暴露（倾向默认隐藏，求救时自动调细）；②多案件并行特权是否增认知负荷；③非桌面场景识别精度是否支撑 V2.0；④商业化路径（种子 ADHD 付费已验证，全人群免费+增值留 PMF 后定）。

---

## 附录 A：Demo 物料清单（现场必备）

- **预录照片**：4-5 张不同乱度桌面 + 对应 after，全脱敏；
- **预设剧本 case**：3 条全链路（拍照→圈框→线索→缩小→验证→盖章→E-1→任务树），含 5 层缩小树，bbox 与原图叠合核对；
- **API/Key**：主 DashScope Qwen2.5-VL + 备 OpenAI/Gemini + 额度预警；
- **视觉素材**：启动徽章/首案章/金章/银章 PNG、相纸+夹子+绳子+吊灯 Lottie、结案报告 9:16 模板、侦探人格标签；
- **文案库**：全量过羞耻度测试、信任制变量轮换表、loading 三档文案；
- **设备**：中端安卓 1 台（三特效 100 次压测）+ Mac 备份 + 热点；
- **隐私五道闸检查**：不落库/TTL 10min/默认本地/手动涂抹/示例桌面开关。

---

## 附录 B：v2.1 交互与视觉改版记录

### 1. 本次核心决策
- 把"先想目标再做"反转为"先行动再委托目标"，确立**双案件入口**（快速开工 / 主动委托）。
- 把案件页从"单张任务卡 + 迷雾"升级为**推理墙渐进照亮**（相纸夹绳 + 吊灯 + 黑雾）。
- 把首页从"全屏相机"改为**三按钮主页面**（相机为王 + 新建案件 + 档案库 + 头像）。
- 新增低摩擦**登录页**（修正 v2.0"不登录"规则）。
- 档案库从普通列表改为**档案文件夹**堆叠。
- 确立**侦探 noir 视觉与美术系统**独立章节。

### 2. 与 v2.0 相比的主要变化
- 新增页面：A 启动页、B 登录页、E-1 委托案件；C/D/E/F/G 重构；H/I 保留。
- 状态机扩展：`creation_mode`、`case_status.draft/analyzing/archived`、`clue_status.blank/generating`、`is_activation_clue`；明确 quick_capture 拍照即 active、commissioned 经 draft→analyzing→active。
- 数据模型新增 `creation_mode`/`case_name`/`materials_ref`/`has_formal_goal`/`is_activation_clue`/`login_mode`/`cold_case_since`/`suspended_since`/`stamp_type`；统一 snake_case 命名（驼峰仅作前端别名）。
- 印章/徽章口径统一：印章=单次墙面反馈（金/银/启动/留档四类），徽章=可收藏实体（证物章+等级徽章）；cold case 搁置发"留档章"不发银章。
- 删除旧规则：首次不登录、首屏即相机、首页只有相机、拍照即完整案件、案件页单张普通任务卡、档案库普通列表。

### 3. 新增页面
A 启动页、B 登录页、E-1 委托案件页。

### 4. 删除/替换的旧流程
- 删：打开即相机、禁止登录、全屏相机首页、拍照立刻形成完整案件。
- 换：单张任务卡 → 推理墙；普通列表档案 → 档案文件夹。

### 5. 尚待验证的产品风险
- 双入口是否增加认知负荷（H8）；
- 登录页是否成为新阻力（游客体验对冲）；
- 委托模式任务树拆解质量是否达标（对标 goblin.tools）；
- 推理墙复杂动画在中端机的性能（MVP 已降级规避）。

### 6. MVP 与后续版本边界
- **MVP**：Logo、登录、三按钮首页、拍照+转场、单绳相纸悬挂、简化吊灯、放大镜扫描、任务卡、盖章、委托表单、2-3 相纸推理墙、黑雾透明度、档案文件夹、状态保存恢复；真跑 VLM/diff/任务树。
- **后续**：大规模复杂推理墙、高级绳索关联、物理摆动、多层灯光、全量徽章、推理墙编辑、多人协作、真 LLM decompose、自动预模糊。

---

**文档结束。本 PRD 为最终版（v3.0），可直接进入周末 MVP 开发与路演准备。**
